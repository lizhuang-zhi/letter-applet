<view class="stamp-container">

  <block wx:for="{{achievementsArr}}" wx:key='index'>
    <!-- 邮票item -->
    <yeo-frame-view class="item-stamp" framePadding='30rpx 30rpx' frameBgColor='{{item.isLock == false ? "var(--LockBgColor)" : "var(--solveLockBgColor)"}}' frameAlignItems='center'
      frameBoxSizing='border-box' frameBordRadius='0rpx'>

      <!-- 未解锁成就 -->
      <block wx:if="{{!item.isLock}}">
        <!-- 未解锁图片 -->
        <view class="left-pic">
          <image src="../../images/lock.png"></image>
        </view>
        <!-- 右侧信息 -->
        <view class="right-info" style="width: 80%;">
          <yeo-frame-view direction='col' frameBgColor='{{item.isLock == false ? "var(--LockBgColor)" : "var(--solveLockBgColor)"}}' frameAlignItems='center'>
            <!-- 右侧部分 -->
            <view class="top-part">
              <text class="tit">{{item.tit}}</text>
              <view class="desc">{{item.desc}}</view>
            </view>
          </yeo-frame-view>
        </view>
      </block>


      <!-- 已解锁成就 -->
      <block wx:else>
        <!-- 左侧配图 -->
        <view class="left-pic">
          <image src="{{item.achievementsPic}}"></image>
        </view>
        <!-- 右侧信息 -->
        <view class="right-info" style="width: 80%;">
          <yeo-frame-view direction='row' frameBgColor='{{item.isLock == false ? "var(--LockBgColor)" : "var(--solveLockBgColor)"}}'>
            <!-- 右侧上部分 -->
            <view class="top-part">
              <text class="tit">{{item.tit}}</text>
              <view class="desc">{{item.desc}}</view>
            </view>
            <!-- 右侧下部分 -->
            <view class="bot-part">
              <yeo-frame-view frameBgColor='{{item.isLock == false ? "var(--LockBgColor)" : "var(--solveLockBgColor)"}}' frameAlignItems='center'>
                <!-- 解锁时间 -->
                <yeo-frame-view frameBgColor='{{item.isLock == false ? "var(--LockBgColor)" : "var(--solveLockBgColor)"}}' frameAlignItems='center'>
                  <view class="lock-pic">
                    <image src="../../images/solve_lock.png"></image>
                  </view>
                  <view class="cont">{{item.lockTime}}</view>
                </yeo-frame-view>
                <!-- 邮票版本号 -->
                <view class="edition">{{item.edition}}</view>
              </yeo-frame-view>
            </view>
          </yeo-frame-view>
        </view>
      </block>

    </yeo-frame-view>
  </block>

</view>